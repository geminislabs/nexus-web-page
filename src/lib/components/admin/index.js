/**
 * Kit de UI reutilizable para el workspace Administración.
 *
 * - AdminWorkspace — shell (sidebar + navbar + switcher)
 * - AdminDashboard — resumen con datos reales de API
 * - WorkspaceSwitcher — Administración ↔ Seguimiento
 * - AlarmBell — historial de alarmas (campana)
 * - StatCard / AdminPageHeader / QuickActionButton — piezas genéricas para nuevas secciones
 *
 * Solo incluir secciones respaldadas por siscom-admin-api.
 */
export {};
